directories:
  buildResources: buildResources
  output: dist
extraResources: ["tools", "firmwares", "libraryes"]

appId: "net.huintech.scratch-coconut-link"
productName: "Scratch COCONUT Link"
publish: # empty provider list = don't publish

fileAssociations:
  ext: sba
  name: Scratch Coconut Project File
  role: Editor
  icon: buildResources/scratch-arduino-file.ico

win:
  icon: buildResources/scratch-arduino-link.ico
  target:
    - nsis

nsis:
  perMachine: false
  oneClick: false
  allowToChangeInstallationDirectory: true
  artifactName: "Scratch-COCONUT-Link-Win-v${version}.${ext}"
  deleteAppDataOnUninstall: true
  createDesktopShortcut: true
  include: buildResources/installer.nsh
